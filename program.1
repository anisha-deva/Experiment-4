//valvar.l %{ #include "y.tab.h" %} %% [a-zA-Z] {return LET;} [0-9] {return DIG;}22   {return yytext[0];} \n {return 0;} %% int yywrap() { return 1; } //valvar.y %{ #include<stdio.h> %} %token LET DIG %% variable: var ; var: var DIG | var LET | LET ; %% int main() { printf("Enter the variable:\n"); yyparse(); printf("Valid variable \n"); return 0; } int yyerror() { printf("Invalid variable \n"); exit(0); } 
